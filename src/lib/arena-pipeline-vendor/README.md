# Arena pipeline (server vendor copy)

Копия `coach-app` модулей split → parse → resolution → review items для **Next.js API** (корневой `tsconfig` исключает `coach-app`, поэтому импорт напрямую невозможен).

При изменении логики pipeline в coach-app — обновить файлы здесь вручную или скриптом копирования.
