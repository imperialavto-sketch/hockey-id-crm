/** Совпадает со строками Prisma kind в CRM (см. src/lib/messenger-kinds.ts). */
export const MESSENGER_KIND = {
  COACH_PARENT_DIRECT: 'coach_parent_direct',
  TEAM_PARENT_CHANNEL: 'team_parent_channel',
} as const;
