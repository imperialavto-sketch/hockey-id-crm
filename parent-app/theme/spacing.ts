/**
 * Unified spacing — only these values across the app.
 * xs=4, sm=8, md=12, lg=16, xl=20, xxl=24, xxxl=32
 */

export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
  /** Screen horizontal padding */
  screenPadding: 16,
  /** Padding below scroll content (above tab bar) */
  screenBottom: 24,
  /** Gap between sections */
  sectionGap: 24,
  /** Gap between cards */
  cardGap: 16,
  /** Vertical list gap */
  listGap: 16,
  /** Grid gap */
  gridGap: 12,
} as const;
