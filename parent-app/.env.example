# API base URL — use your Mac's LAN IP (e.g. ifconfig | grep "inet ")
# localhost/127.0.0.1 do not work on physical iPhone
EXPO_PUBLIC_API_URL=http://192.168.31.59:3000

# Expo project ID for push notifications (from expo.dev project settings)
EXPO_PUBLIC_EXPO_PROJECT_ID=your-project-id
